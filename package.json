{
  "name": "exceed-portal",
  "version": "0.1.0",
  "private": true,
  "description": "Exceed Real Estate internal staff portal — admin scripts only. The portal itself is static HTML and runs from GitHub Pages, no build step.",
  "scripts": {
    "sync-drive": "node scripts/sync-drive.js"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "googleapis": "^144.0.0"
  }
}
